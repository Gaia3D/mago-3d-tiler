plugins {
    id 'java'
    id 'java-library'
    id 'maven-publish'
}

repositories {
    maven { url "https://repo.osgeo.org/repository/release/" }
    mavenCentral()
}

dependencies {

}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
            artifactId = 'mago-common'
        }
    }
    repositories {
        maven {
            name = "github"
            url = uri("https://maven.pkg.github.com/gaia3d/mago-3d-tiler")
            credentials {
                username = findProperty("gpr.user") ?: System.getenv("GITHUB_ACTOR")
                password = findProperty("gpr.key") ?: System.getenv("GITHUB_TOKEN")
            }
        }
    }
}

javadoc {
    enabled = false
}

test {
    useJUnitPlatform {
        includeTags 'release'
    }
}